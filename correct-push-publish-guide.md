# 正确的代码推送和发布流程

## 🔍 问题分析

你发现的问题非常关键：

- **`git push`** → 只触发质量检查，跳过发布
- **`git push --tags`** → 触发发布，但可能不是最新代码

## ✅ 正确的操作流程

### 步骤1: 更新代码并提交

```bash
# 1. 更新代码（修改功能、修复bug等）
# 2. 提交代码
git add .
git commit -m "feat: 添加新功能"

# 3. 先推送代码到 GitHub
# 这会触发 quality-check 作业，确保代码质量
# 但 publish 作业会被跳过（这是正确的）
git push
```

### 步骤2: 创建并推送标签

```bash
# 4. 创建版本标签
# 确保标签指向最新的提交
git tag v1.7.8

# 5. 推送标签到 GitHub
# 这会触发 publish 作业，发布到 npm
git push --tags
```

## 🎯 为什么这个流程正确？

### `quality-check` 作业

- 触发条件：`push` 到 main/master 分支
- 作用：运行测试、代码检查、构建
- 目的：确保代码质量

### `publish` 作业

- 触发条件：推送 `v*` 标签
- 作用：发布到 npm，创建 GitHub Release
- 关键：**使用 `ref: ${{ github.ref_name }}` 确保检出标签对应的提交**

## 🔧 修复的关键点

我已经修复了工作流配置：

1. **移除了分支切换逻辑** - 不再需要切换到 master/main
2. **使用标签引用检出** - 确保发布的是标签对应的代码
3. **保持 `--no-git-checks`** - 避免 Git 检查问题

## 📋 验证流程

推送完成后，检查：

1. **GitHub Actions**: https://github.com/planarcat/js-toolkit/actions
   - 应该看到两个作业：`quality-check` 和 `publish`
2. **npm 包**: https://www.npmjs.com/package/@planarcat/js-toolkit
   - 版本应该更新到 1.7.8
3. **GitHub Releases**: https://github.com/planarcat/js-toolkit/releases
   - 应该看到新的 Release

## 💡 自动化版本更新（推荐）

使用自动版本更新脚本：

```bash
# 自动更新版本、生成文档、创建标签
pnpm run bump-version

# 然后推送代码和标签
git push
git push --tags
```

现在你的自动化发布流程应该能正常工作了！
