# 任务1：优化 formatDate 函数的 JSDoc 注释

**修改内容**：
- 将现有 JSDoc 中的「示例」与「参数说明」对换位置
- 为每个参数提供清晰的默认值说明
- 为 `options` 参数添加更详细的属性说明，包括每个属性的作用、类型和默认值

# 任务2：修改显示周几的模式

**修改内容**：
1. 保留并增强 `dd` 标记功能，使其能够直接在格式化字符串中使用，例如 `'dd HH:mm'` 生成 `'周一 14:30'`
2. 移除 `showWeekday` 和 `weekdayFormat` 选项的使用逻辑（保留类型定义以保持兼容性）
3. 更新示例代码，展示新的标记模式使用方法
4. 保持向后兼容性

**实现步骤**：
1. 更新 `formatDate` 函数的 JSDoc 注释，调整示例和参数说明的顺序，并添加详细的默认值
2. 修改 `formatDate` 函数的实现，移除 `showWeekday` 相关的逻辑
3. 确保 `dd` 标记能够正确显示完整的周几名称（如 "周一"、"Tuesday" 等）
4. 更新示例代码，展示新的周几标记使用方法
5. 运行测试确保功能正常