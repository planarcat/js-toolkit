# 测试规则

## 1. 测试框架的详细要求
- 使用 ts-node 进行类型安全的测试运行
- 所有功能必须通过 TypeScript 编译检查

## 2. 测试文件更新规范
- 每次修改后，需要针对修改更新测试文件
- 新增功能时，需要添加对应的测试用例
- 修改现有功能时，需要更新相关测试用例确保覆盖修改点
- 删除功能时，需要移除对应的测试用例
- 所有测试用例必须通过，确保修改不会破坏现有功能